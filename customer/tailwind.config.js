/** @type {import('tailwindcss').Config} */
export default {
  theme: {
    extend: {
      fontFamily: {
        sigmar: ["sigmar", "serif"], // Đặt tên giống với `font-family` trong CSS
      },
    },
  },
  plugins: [],
};
